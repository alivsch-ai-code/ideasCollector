// Copy this file to firebase-config.js and fill in your Firebase project's config.
// Then deploy the `public` folder to Netlify. The frontend will use Firestore
// for storing posts and likes. If firebase-config.js is missing, the app will
// fall back to local REST endpoints at /api/* (for local Express testing).

// Example (replace with your project's values):
/*
window.firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project-id.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID"
};
*/

// Save as firebase-config.js with real values.
